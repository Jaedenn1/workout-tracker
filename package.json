{
  "name": "workout-tracker",
  "version": "1.9.5",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test:live": "rm -rf .tmp-live-test && tsc --ignoreConfig --rootDir src --module node16 --moduleResolution node16 --target es2022 --esModuleInterop --skipLibCheck --outDir .tmp-live-test src/lib/liveTraining.ts && node scripts/live-reliability.test.cjs && rm -rf .tmp-live-test"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "dexie": "4.4.5",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "typescript": "latest"
  }
}
